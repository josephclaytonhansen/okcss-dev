const authMiddleware=async(s,e)=>{let i=s.session.passport.user;if(s.session.permissions)return"worm"===s.session.permissions;{const r=await e.findById(i);return s.session.permissions=r.permissions,"worm"===r.permissions}};export default authMiddleware;