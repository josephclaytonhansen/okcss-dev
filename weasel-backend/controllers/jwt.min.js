import jwt from"jsonwebtoken";import dotenv from"dotenv";dotenv.config();const secret=process.env.JWT_SECRET;export const createToken=e=>jwt.sign({email:e.email,organization:e.organization,ward:e.ward},secret,{expiresIn:"30m"});export const verifyToken=e=>jwt.verify(e,secret);