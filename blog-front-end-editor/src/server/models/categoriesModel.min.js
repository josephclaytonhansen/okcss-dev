import mongoose from"mongoose";const categorySchema=new mongoose.Schema({name:{type:String,index:!0}}),Category=mongoose.model("Category",categorySchema);Category.createIndexes();export default Category;