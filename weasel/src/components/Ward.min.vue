<script setup>import{ref,watch} from 'vue' import PersonCard from './PersonCard.vue'const props=defineProps({ size:{String, default: 'full'}, colorClass: String, contacts: Array, ward: String, events: Array, tools: Array, worship:{ location: String, time: String, googleMapsLink: String, image:{ src: String, alt: String, width: String, class: String}}})const currentTab=ref('contacts')function tabChanged(tab){ console.log(tab) currentTab.value=tab}function setPersonClassesByContactSize(size){ if (size==='full'){ return 'row person col-6 right fwc'} else{ return 'row person col-3 right fwc'}}watch(currentTab, tabChanged)</script><template><div class="row wrap flex-between"><PersonCard v-for="contact in props.contacts" :key="contact.id" :name="contact.name" :image="contact.image" :position="contact.position" :email="contact.email" :phone="contact.phone" :classes="setPersonClassesByContactSize(contact.size)" :size="contact.size"/></div></template>